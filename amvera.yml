meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  skip: false
  additionalCommands:
    - npm install
    - npm run build
run:
  command: npm run start
  persistenceMount: /data
  containerPort: "3001"
  servicePort: "80"
