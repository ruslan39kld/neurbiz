meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  skip: false
  additionalCommands: "npm ci"

artifacts:
  "**": /

run:
  scriptName: null
  scriptArguments: null
  nodeArguments: null
  command: npx tsx server.ts 
  persistenceMount: /data
  containerPort: "3001"
  servicePort: "80"