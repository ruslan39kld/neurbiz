// RAG search removed — knowledge_base was in Supabase which is no longer used.
// The chat-bot (botService.ts) still works via GigaChat / Claude API.
export const searchKnowledge = async (_query: string): Promise<string> => '';
